-- 回退 0011。
--
-- 【只删索引，不回滚 updated_at 的值】把已更新的时间戳改回去需要一个
-- "原来是什么"的备份，而那份备份不存在；而保留它们是无害的——0010 之前
-- 的代码只是不读它。相比之下"随便减一点回退"会制造一批看起来像真的、
-- 实际上没有意义的时间戳。
DROP INDEX IF EXISTS conversations_updated_at_id_idx;
