# ConGoRAG 启动包的环境变量（issue #73）
#
# 【怎么用】复制成 .env，改掉要改的那几项：
#     cp .env.example .env
# docker compose 会自动读**与 docker-compose.yml 同目录**的 .env 文件，
# 所以这个文件必须和 docker-compose.yml 待在一起，不要挪到别处。
#
# 【只有一项是必填的】CONGORAG_VERSION。其它全部有默认值，删掉也能起来。
# 下面每一行都写明了"不填会怎样"。
#
# ── 必填 ────────────────────────────────────────────────────────

# 镜像 tag。启动包里 docker-compose.yml 引用的三个镜像是
#   congorag-api:<版本>  congorag-worker:<版本>  congorag-migrate:<版本>
# 它们由 `docker load` 从 images/*.tar.gz 里导进来。
# 【填错的表现】compose 报 "image congorag-api:3.0 not found"——那不是镜像坏了，
# 是 tag 对不上；`docker images | grep congorag` 能看到实际导入的 tag。
# 【为什么没有默认值】给一个默认版本号，会让"我到底装的是哪个版本"变成
# 一个要翻文件才知道的问题。宁可 here 报一次错。
CONGORAG_VERSION=3.0

# ── 数据库连接 ──────────────────────────────────────────────────
#
# 【注意地址里的主机名是 postgres，不是 127.0.0.1】这是 compose 的服务名，
# 在容器之间的网络里能解析。写成 127.0.0.1 的话解析到 api 容器**自己**，
# 表现是 api 起来后一直连不上库、报 connection refused。
#
# 这一项在 docker-compose.yml 里有同样的默认值，所以删掉也不会坏；
# 列在这里是因为升级/回滚脚本要读它来判断连的是哪个库。
CONGORAG_DB_URL=postgres://postgres:congorag@postgres:5432/congorag?sslmode=disable

# 数据库密码。**只在数据卷第一次初始化时生效**——改了它再重启，密码还是旧的。
# 已经在跑的实例要改密码得进容器 ALTER USER，不是改这里。
CONGORAG_PG_PASSWORD=congorag

# ── 网络 ────────────────────────────────────────────────────────

# api 在**容器内**的监听地址。
#
# ══════════════════════════════════════════════════════════════════
# 【这一项不要改成 127.0.0.1，除非你确定自己在做什么】
#
# 程序内部的默认值是 127.0.0.1:3210（internal/platform/config.go，出于安全
# 考虑）。容器里用那个默认值的话，宿主机的端口映射**转发不进来**，而症状
# 极具误导性：容器正常启动、日志照常打印 `listening`、健康检查也是通过的，
# 只有从外面连是 connection refused。
#
# 所以 docker-compose.yml 里显式覆盖成了 0.0.0.0:3210。这一行写出来是为了
# 让你看见它存在，而不是为了让你改它——"容器内绑全网卡"和"对外暴露"是两件事，
# 真正的暴露面由下面 CONGORAG_PORT 那一行的 127.0.0.1 前缀决定。
# ══════════════════════════════════════════════════════════════════
CONGORAG_LISTEN_ADDR=0.0.0.0:3210

# api 映射到**宿主机**的端口。浏览器打开 http://127.0.0.1:3210 就是它。
# 只绑宿主机回环——局域网上的其他机器连不进来（方案 §1 的安全基线）。
# 想换个端口（比如 3210 被占用）改这里，容器内部不用动。
CONGORAG_PORT=3210

# PostgreSQL 映射到宿主机的端口，供排障时用 psql 连（也可以直接删掉）。
# 用 `docker compose exec postgres psql -U postgres -d congorag` 就不需要它。
CONGORAG_PG_PORT=5432

# ── 常用可选项 ──────────────────────────────────────────────────

# debug | info | warn | error。排查上传/检索问题时填 debug。
CONGORAG_LOG_LEVEL=info

# 单次上传允许的最大请求体字节数，默认 32 MiB（= 33554432）。
# 【填 0 或非法值不会变成"不限"】internal/platform/config.go 会把非法值退回
# 默认值——这个上限存在的意义就是拦住超大请求体，静默关掉它是不可接受的。
CONGORAG_MAX_UPLOAD_BYTES=33554432

# ── 不在这里的配置项 ────────────────────────────────────────────
#
# 模型（provider / api key / 模型名）**不在这个文件里配**，在界面上的
# 「设置 → 模型」里配：api key 会用主密钥加密后存进数据库，而不是散落在
# 环境变量和 shell 历史里。主密钥文件在 congorag-data 卷里（/data/master.key），
# 首次启动时自动生成——**它丢了的话，库里那些加密过的 key 全部作废**，
# 所以备份要连着这个卷一起备（见 docs/upgrading.md）。
#
# 数据库和原始文件的目录也不在这里配：容器里它们的路径是固定的
# （服务名 postgres / 卷路径 /data），改了不会生效，只会让人以为生效了。
